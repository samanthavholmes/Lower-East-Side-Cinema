<div class = "form">
<%= bootstrap_form_for @rating do |f| %>
  <% if @rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
      <% @rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.hidden_field :film_id, value: @film.id %>
  </div>

  <div class="field">
    <%= f.number_field :stars, in: 0..5, step: 0.5%>
  </div>
  <div class="field">
    <%= f.text_field :review %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit' %>
  </div>
<% end %>
</form>